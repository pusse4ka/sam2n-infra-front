name: 'Install Terraform'
description: 'Installs terraform'
inputs:
  terraform-version:
    description: 'Terraform version'
    required: false
    default: '1.4.0'
runs:
  using: "composite"
  steps:
    - run: export TF_VERSION=${{ inputs.terraform-version }}
      shell: bash
    - run: sudo $GITHUB_ACTION_PATH/install_tf.sh --version=$TF_VERSION
      shell: bash